[project]
name = "python-socketio-docs"
version = "0.0.0"
dependencies = [
    "sphinx>=7.3.7",
]
requires-python = ">=3.11"

[build-system]
requires = ["pdm-pep517>=1.0.0"]
build-backend = "pdm.pep517.api"

[tool.pdm]
